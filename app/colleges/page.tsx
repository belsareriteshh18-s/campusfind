'use client'
import Link from 'next/link'
import { useState } from 'react'
import { ArrowLeft, Bookmark, MapPin, Search, Star } from 'lucide-react'
import { colleges } from '@/data/colleges'
export default function CollegesPage(){const [q,setQ]=useState('');const [kind,setKind]=useState('All types');const result=colleges.filter(c=>(kind==='All types'||c.type===kind)&&(!q||`${c.name} ${c.city} ${c.courses.join(' ')}`.toLowerCase().includes(q.toLowerCase())));return <main className="min-h-screen bg-background"><header className="border-b"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link href="/" className="font-semibold">CampusFind</Link><Link href="/saved" className="text-sm text-muted-foreground">Saved colleges</Link></div></header><div className="mx-auto max-w-7xl px-5 py-12"><Link href="/" className="flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft/> Back home</Link><h1 className="mt-8 text-4xl font-semibold tracking-tight">Explore colleges</h1><p className="mt-2 text-muted-foreground">Browse {colleges.length} colleges across India.</p><div className="mt-8 flex flex-col gap-3 md:flex-row"><div className="flex flex-1 items-center gap-3 rounded-xl border bg-card px-4"><Search className="text-muted-foreground"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search by college, city, or course" className="w-full bg-transparent py-3 outline-none"/></div><select value={kind} onChange={e=>setKind(e.target.value)} className="rounded-xl border bg-card px-4"><option>All types</option><option>Government</option><option>Private</option></select></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{result.map(c=><Link key={c.id} href={`/colleges/${c.id}`} className="rounded-2xl border bg-card p-5 transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex justify-between"><span className="grid size-12 place-items-center rounded-xl text-sm font-bold" style={{backgroundColor:c.accent}}>{c.shortName.slice(0,3)}</span><Bookmark className="text-muted-foreground"/></div><h2 className="mt-5 font-semibold">{c.name}</h2><p className="mt-1 flex items-center gap-1 text-sm text-muted-foreground"><MapPin/> {c.city}, {c.state}</p><div className="mt-5 flex gap-4 text-sm"><span className="flex items-center gap-1 font-semibold"><Star fill="currentColor" className="text-amber-500"/> {c.rating}</span><span className="text-muted-foreground">{c.type}</span><span className="text-muted-foreground">{c.fees}</span></div></Link>)}</div>{!result.length&&<p className="py-20 text-center text-muted-foreground">No colleges match those filters.</p>}</div></main>}
