import Link from 'next/link'
import { ArrowLeft, MapPin, Star, CheckCircle2 } from 'lucide-react'
import { colleges, getCollege } from '@/data/colleges'
export function generateStaticParams(){return colleges.map(c=>({id:c.id}))}
export default async function CollegeDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=getCollege(id);if(!c)return <main className="p-10">College not found</main>;return <main className="min-h-screen bg-background"><header className="border-b"><div className="mx-auto flex max-w-5xl justify-between px-5 py-4"><Link href="/" className="font-semibold">CampusFind</Link><Link href="/colleges" className="text-sm text-muted-foreground">Explore colleges</Link></div></header><div className="mx-auto max-w-5xl px-5 py-12"><Link href="/colleges" className="flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft/> Back to colleges</Link><div className="mt-10 flex flex-col gap-6 md:flex-row md:items-end md:justify-between"><div><span className="grid size-16 place-items-center rounded-2xl text-lg font-bold" style={{backgroundColor:c.accent}}>{c.shortName.slice(0,3)}</span><h1 className="mt-5 text-4xl font-semibold tracking-tight">{c.name}</h1><p className="mt-2 flex items-center gap-2 text-muted-foreground"><MapPin/> {c.city}, {c.state} · {c.type}</p></div><div className="flex items-center gap-2 rounded-xl border bg-card px-4 py-3"><Star fill="currentColor" className="text-amber-500"/><strong>{c.rating}</strong><span className="text-sm text-muted-foreground">from {c.reviews} reviews</span></div></div><div className="mt-10 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border bg-card p-5"><p className="text-sm text-muted-foreground">Average fees</p><p className="mt-2 text-2xl font-semibold">{c.fees}</p></div><div className="rounded-2xl border bg-card p-5"><p className="text-sm text-muted-foreground">Established</p><p className="mt-2 text-2xl font-semibold">{c.established}</p></div><div className="rounded-2xl border bg-card p-5"><p className="text-sm text-muted-foreground">Popular courses</p><p className="mt-2 font-semibold">{c.courses[0]}</p></div></div><section className="mt-10 max-w-3xl"><h2 className="text-2xl font-semibold">About {c.shortName}</h2><p className="mt-4 leading-7 text-muted-foreground">{c.description}</p><div className="mt-6 flex flex-wrap gap-2">{c.tags.map(t=><span key={t} className="flex items-center gap-2 rounded-full bg-muted px-3 py-1.5 text-sm"><CheckCircle2 className="text-primary"/> {t}</span>)}</div></section></div></main>}
